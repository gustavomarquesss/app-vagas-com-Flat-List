import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  titulo: {
    fontSize: 24,
    color: 'red',
    textAlign: 'center',
  },

  tela: {
    marginTop: 40,
  },
});

export { styles };
